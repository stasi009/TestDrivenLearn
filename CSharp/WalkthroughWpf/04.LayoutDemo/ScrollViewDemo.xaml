<Page
  xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
  xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml" mc:Ignorable="d" xmlns:d="http://schemas.microsoft.com/expression/blend/2008" xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" d:DesignHeight="208" d:DesignWidth="187">
    <!--<ScrollViewer>-->
    <ScrollViewer VerticalScrollBarVisibility="Auto" HorizontalScrollBarVisibility="Auto">
        <!--note: when stackpanel is shrinked, then some of its children will disappear from the screen, so there is need to add the scroll bar-->
        <StackPanel>
            <Button Width="200" Height="50">explict size</Button>
            <Button Margin="20 10">with margin</Button>
            <Button Padding="10 4">with padding</Button>
            <Button HorizontalAlignment="Left">left alignment</Button>
            <Button HorizontalAlignment="Right">right alignment</Button>
            <Button HorizontalAlignment="Center">center alignment</Button>
            <Button HorizontalAlignment="Stretch">stretch alignment (default)</Button>
        </StackPanel>
    </ScrollViewer>
</Page>