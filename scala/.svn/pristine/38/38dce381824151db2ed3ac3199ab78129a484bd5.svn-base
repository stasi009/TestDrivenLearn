package test

import org.scalatest.Spec
import java.util.Calendar

sealed class DateTimeTest extends Spec {
  
  object `basic API usage` {
    
    def `test clear` = {
      val calendar = Calender.getInstance()
    }//def
    
  }//object
  
}