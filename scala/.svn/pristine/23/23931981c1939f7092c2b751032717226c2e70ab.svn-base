package demo

import io.Source

object FileIoDemo extends App {
  
  def demoReadText() = {
    val source = Source.fromFile("PythonZen.txt")
    val lniterator = source.getLines
    lniterator.zipWithIndex.foreach {case(ln,index)=>println(s"[$index]:$ln")}
  }//def
  
  override def main(args: Array[String]) = {
    demoReadText()
  }//def

}